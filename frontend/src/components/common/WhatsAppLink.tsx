import { MessageCircle } from 'lucide-react'; import useLanguage from '../../hooks/use-language'; import useWebsiteContent from '../../hooks/use-website-content'; import { whatsappUrl } from '../../services/whatsapp'
export default function WhatsAppLink({booking=false,className='whatsapp-link'}:{booking?:boolean;className?:string}){const{language,tr}=useLanguage();const{content}=useWebsiteContent();const url=whatsappUrl(language,content.whatsappNumber);const label=tr(booking?'askBooking':'contactWhatsApp');return <a className={className} href={url} target="_blank" rel="noreferrer" aria-label={label}><MessageCircle size={18}/><span>{label}</span></a>}
