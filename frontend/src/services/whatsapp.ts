export function whatsappUrl(language: 'en'|'ar', configuredNumber?: string) { const number=configuredNumber || import.meta.env.VITE_WHATSAPP_NUMBER || '972556800545'; const message=language==='ar'?'مرحبًا، أريد الاستفسار عن حجز مكياج عروس.':'Hello, I would like to ask about a bridal makeup booking.'; return `https://wa.me/${number.replace(/\D/g,'')}?text=${encodeURIComponent(message)}` }
